# Изменения навигации, логотипа и связи

## Что будет сделано

1. **Страница «Цены» удаляется** — главная страница уже содержит весь каталог (3 стабилизатора и 6 дополнительных товаров с ценами, фото и описаниями). Сама страница `/prices` будет удалена.
   - Вкладка «Цены» в шапке остаётся, но теперь ведёт на главную и прокручивает вниз к разделу «Стабилизаторы».

2. **Шапка: телефон заменяется на мессенджеры**
   - Вместо номера +7 911 923-47-91 — значок WhatsApp (зелёный) с подписью «WhatsApp» и значок Telegram (синий) с подписью «Telegram».
   - Telegram ведёт на https://t.me/timber827, WhatsApp — на https://www.ixbt.com/ (ссылка из вашего ответа; её легко заменить позже).

3. **Всплывающее окно связи**
   - Красные кнопки «Заказать или получить консультацию», «Заказать звонок» и «Купить» открывают красивое модальное окно.
   - Внутри: заголовок «Выберите удобный способ связи с мастером:» и две большие кнопки — зелёная «Написать в WhatsApp» и синяя «Написать в Telegram», каждая ведёт по соответствующей ссылке.
   - Старая форма заявки (имя + телефон) удаляется.

4. **Логотип RR-STAB** — текст «СТАБ ПАРТНЁР» заменяется на «RR-STAB» в шапке и в подвале (везде, где встречается). Стиль: «RR-» белым, «STAB» красным, тот же крупный размер.

5. **Прочее**
   - Ссылка «Признаки неисправности» в шапке убирается (блок был заменён каталогом) — вместо неё в меню остаются «Цены» (прокрутка к каталогу), «Преимущества», «Контакты».
   - Заголовки и описания страниц для поисковых систем обновляются под RR-STAB.

## Технические детали
- Удаляется файл `src/routes/prices.tsx`; ссылки на него заменяются якорем `#stabilizers` на главной.
- Раздел каталога на главной получит `id="stabilizers"` и отступ прокрутки под липкую шапку.
- Обновляются `src/components/site-shell.tsx` (шапка, подвал, модальное окно) и `src/routes/index.tsx` (якорь, метаданные).
- Проверка: сборка, открытие `/` на компьютере и телефоне, работа прокрутки и обеих кнопок мессенджеров, недоступность `/prices`.
